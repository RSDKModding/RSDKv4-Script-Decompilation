// ----------------------------------
// RSDK Project: Sonic CD
// Script Description: R8 Setup Object
// Script Author: Christian Whitehead/Hunter Bridges
// Unpacked by Rubberduckycooly's script unpacker
// ----------------------------------

// ========================
// Aliases
// ========================

// Aliases
private alias object.value0 : object.paletteTimer
private alias object.value1 : object.currentPalette
private alias object.value2 : object.vGearFrame
private alias object.value3 : object.hCylinderFrame
private alias object.value4 : object.vCylinderFrame
private alias object.value5 : object.lightsFrame

// Reserved Object Slot Aliases
private alias 21 : SLOT_R8SETUP

// ========================
// Events
// ========================

event ObjectUpdate
	object.rotation++
	object.rotation %= 144

	temp0 = object.vGearFrame
	temp0 %= 7
	if temp0 == 0
		temp0 = object.vGearFrame
		temp0 /= 7
		temp0 <<= 1
		temp0 += 600
		Copy16x16Tile(444, temp0)
		temp0++
		Copy16x16Tile(445, temp0)
	end if

	object.vGearFrame++
	object.vGearFrame %= 28

	temp0 = object.hCylinderFrame
	temp0 &= 3
	if temp0 == 0
		temp0 = object.hCylinderFrame
		temp0 >>= 2
		temp0 <<= 1
		temp0 += 608
		Copy16x16Tile(446, temp0)
		temp0++
		Copy16x16Tile(447, temp0)
	end if

	object.hCylinderFrame++
	object.hCylinderFrame &= 15

	temp0 = object.vCylinderFrame
	temp0 %= 5
	if temp0 == 0
		temp0 = object.vCylinderFrame
		temp0 /= 5
		temp0 <<= 2
		temp0 += 616
		Copy16x16Tile(448, temp0)
		temp0++
		Copy16x16Tile(449, temp0)
		temp0++
		Copy16x16Tile(450, temp0)
		temp0++
		Copy16x16Tile(451, temp0)
	end if
	
	object.vCylinderFrame++
	object.vCylinderFrame %= 20

	temp0 = object.lightsFrame
	temp0 &= 7
	if temp0 == 0
		temp0 = object.lightsFrame
		temp0 >>= 3
		temp0 += 632
		Copy16x16Tile(452, temp0)
	end if

	object.lightsFrame++
	object.lightsFrame %= 56

	object.paletteTimer++
	if object.paletteTimer == 5
		object.paletteTimer = 0
		object.currentPalette++
		object.currentPalette %= 6
		temp0 = object.currentPalette
		temp0 += 639
	end if

	SetActivePalette(object.currentPalette, 0, screen.ysize)
end event


event ObjectStartup
	object[SLOT_R8SETUP].type = TypeName[R8 Setup]
	object[SLOT_R8SETUP].priority = PRIORITY_ACTIVE

	CopyPalette(0, 0, 1, 0, 255)
	CopyPalette(0, 0, 2, 0, 255)
	CopyPalette(0, 0, 3, 0, 255)
	CopyPalette(0, 0, 4, 0, 255)
	CopyPalette(0, 0, 5, 0, 255)

	temp0 = 0
	temp1 = 0
	temp2 = 32
	while temp0 < 6
		LoadPalette("R81C_PalCycle.act", temp0, 160, temp1, temp2)
		temp0++
		temp1 += 32
		temp2 += 32
	loop
end event


// ========================
// Editor Events
// ========================

event RSDKDraw
	DrawSprite(0)
end event


event RSDKLoad
	LoadSpriteSheet("Global/Display.gif")
	SpriteFrame(-16, -16, 32, 32, 1, 143) // #0 - "Script" Icon
	
	SetVariableAlias(ALIAS_VAR_PROPVAL, "unused")
end event
